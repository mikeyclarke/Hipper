{% for activity in activity_feed %}
    {% include 'activity/_' ~ activity.type ~ '_activity.twig' with {
        activity: activity,
        organization: organization,
        current_path: current_path|default(app.request.getPathInfo()),
    } only %}
{% endfor %}
