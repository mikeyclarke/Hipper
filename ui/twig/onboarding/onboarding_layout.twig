<!doctype html>
<html lang="en">
    <head>
        <link rel="dns-prefetch" href="{{ asset_domain }}">
        <meta charset="utf-8">
        <title>{% if html_title is defined %}{{ html_title }} – {% endif %}Hipper</title>
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link rel="stylesheet" href="{{ asset('signup.css') }}" crossorigin="anonymous">
        <meta name="csrf" class="js-csrf" content="{{ csrf_token }}">
    </head>

    <body>
        {% include 'icon_sprite.twig' %}

        {% block content %}{% endblock %}

        <script src="{{ asset('signup.js') }}" crossorigin="anonymous"></script>
    </body>
</html>
